# Railway Backend Environment Variables
# Copy these to your Railway service settings

# Database connection string (provided by Railway PostgreSQL plugin)
DATABASE_URL=postgresql://user:password@host:port/database

# JWT secret for session tokens (generate a secure random string)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-here

# Allowed CORS origins (comma-separated, include your frontend domain)
# Example: https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=https://your-frontend-domain.vercel.app

# Port (Railway provides this automatically, no need to set)
# PORT=3000

# Rust logging level
RUST_LOG=info,bigtwo=debug
